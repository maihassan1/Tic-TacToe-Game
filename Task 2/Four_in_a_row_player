#include <iostream>
#include"C:\Users\مول الاصدقاء للاتسوق\CLionProjects\CS\BoardGame.h"
using namespace std;
Four_in_a_row_BoardPlayer::Four_in_a_row_BoardPlayer (int order, char symbol): Player(symbol) {
    cout << "Welcome player " << order << endl;
    cout << "Please enter your name: ";
    cin >> name;
    this->symbol = symbol;
}
void Four_in_a_row_BoardPlayer::get_move (int& x, int& y)  {
    cout << "\nPlease enter your move column (0 to 6) and row (0 to 5) separated by spaces: ";
    cin >> x >> y;
}
string Four_in_a_row_BoardPlayer::to_string(){
    return "Player: " + name ;
}
char Four_in_a_row_BoardPlayer::get_symbol() {
    return symbol;
}
